"use strict";
let name = "Ahsan"; //to save the name
console.log(name.toLowerCase()); //for lower case
console.log(name.toUpperCase()); //for upper case
console.log(name.charAt(0).toUpperCase() + name.slice(1).toLowerCase()); //for title case
